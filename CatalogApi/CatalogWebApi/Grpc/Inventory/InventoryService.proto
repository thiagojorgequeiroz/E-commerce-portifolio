syntax = "proto3";

option csharp_namespace = "CatalogWebApi.Grpc.Inventory";

package inventory;

service InventoryService {
    rpc CheckStock(CheckStockRequest) returns (CheckStockResponse);
}

message CheckStockRequest {
    string productId = 1;
    int32 quantity = 2;
}

message CheckStockResponse {
    bool available = 1;
}